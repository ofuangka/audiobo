import { HyphenZeroesPipe } from './hyphen-zeroes.pipe';

describe('HyphenZeroesPipe', () => {
  it('create an instance', () => {
    const pipe = new HyphenZeroesPipe();
    expect(pipe).toBeTruthy();
  });
});
